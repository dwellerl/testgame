# AGENTS.md — 给 AI 助手的项目说明书

> 任何 AI 助手在本项目干活前，先读这个文件。它定义了项目背景、规矩和工作方式，让 AI 一进来就"懂行"，减少来回解释的上下文消耗。

---

## 项目是什么

《残响轮回》：回合制 roguelike RPG。时间循环守钟人题材，三层咬合设计（单局构筑 / 跨局养成 / 叙事轮回）。完整设计见 `设计文档.md`，技术方案见 `技术实现与MVP路线.md`。

## 技术栈（已拍板）

- **语言**：TypeScript（严格模式）
- **构建/开发**：Vite（浏览器即运行，热更新）
- **测试**：Vitest（/core 逻辑必须有单元测试）
- **表现层（后期）**：先用最简 DOM/HTML UI 验证好玩度，正式版用 PixiJS 或 Phaser 加画面
- **包管理**：pnpm（或 npm）
- 详见决策日志 D-001。后续若需迁移 Godot，核心逻辑（/core）可平移。

## 你（AI）该怎么工作

1. **先读上下文，再动手**：每次任务开始，至少读 `进度看板.md` 了解当前状态；按任务需要读相关设计/代码文件。不要读无关文件来"凑保险"。
2. **一次只做一个任务**：本会话聚焦用户声明的单一任务，不要主动扩散去做别的模块。
3. **任务结束要沉淀**：完成后更新 `进度看板.md`（标记完成、写下一步）；有关键决策时追加到 `决策日志.md`。
4. **遵守工程约定**（见下）。

## 工程约定

- **数据驱动**：技能/遗物/敌人/事件都是 `/data` 下的 JSON。新增内容优先加数据，不改引擎代码。
- **分层架构**：
  - `/core` 纯逻辑，不依赖 UI/引擎，必须可独立单元测试
  - `/content` 加载 /data
  - `/ui` 表现层
  - `/save` 存档
- **写逻辑就写测试**：`/core` 的功能要配单元测试验证正确性。
- **种子化随机**：所有随机走统一的 `core/rng`，禁止直接用语言内置随机，保证可复现。
- **小步提交**：每完成一个可验证的小块就 Git commit，信息写清做了什么。

## 关键设计红线（别违反）

- 跨局永久养成主要给"更多选择"（新技能/遗物），**克制纯数值碾压**，否则毁掉 roguelike 挑战性。
- 保留敌人"意图预示"系统，这是回合制博弈深度的核心。
- 随机性要让玩家觉得"死于决策"而非"死于运气"（保底、奖励可选、种子化）。
- MVP 阶段坚决砍范围：先验证核心循环好玩，不要过早做美术/堆内容。

## 数据 schema 参考

技能 / 遗物 / 敌人的 JSON 格式见 `技术实现与MVP路线.md` 第二节。需要扩展效果类型时，先在该文档登记新枚举。
