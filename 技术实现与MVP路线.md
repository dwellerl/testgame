# 《残响轮回》技术实现与 MVP 路线

> 面向「会一点编程、主要靠 AI 辅助写代码」的独立开发者。核心原则：选 AI 训练数据最多、文档最全的成熟技术栈，让 AI 给你最靠谱的代码；用数据驱动设计，把「内容」和「逻辑」分开，方便快速迭代和让 AI 批量生成内容。

---

## 一、技术栈选型

回合制 + 独立开发 + AI 辅助，推荐按你的偏好二选一：

### 方案 A（最推荐）：Godot + GDScript / C#

- **优点**：免费开源、轻量、上手快、文档优秀、社区活跃；GDScript 语法接近 Python，AI 写起来很顺；2D 回合制完全够用，导出多平台（PC/移动/Web）方便。
- **AI 友好度**：高。Godot 4 的资料 AI 掌握良好。
- **适合**：想做带画面的 2D 游戏，长期维护。

### 方案 B（最快出原型）：Web 技术栈（TypeScript + 前端框架）

- **优点**：你只要会一点 JS/TS 就能上手；回合制是纯逻辑 + UI，用 Web 做原型极快；天然跨平台（浏览器即可玩）；AI 对 TS/React 的代码生成质量极高。
- **推荐组合**：TypeScript + React（或 Svelte）做 UI，纯逻辑层用 TS 写（不依赖框架，方便测试）。需要画面再上 PixiJS / Phaser。
- **适合**：先快速验证「核心循环好不好玩」，甚至可以先做纯文字版。

### 方案 C：Python（仅做原型/文字版）

- 用 Python 快速写一个**命令行文字版**验证战斗和构筑数值，零美术成本。验证完再用 A 或 B 重做。
- 这是「先验证好玩再投入」思路下成本最低的起点。

> **给你的建议**：既然「主要靠 AI 辅助」且「先验证好玩」，我建议**先用方案 C 或 B 做一个能跑通核心循环的极简原型**（甚至纯文字/纯 UI 无美术），验证战斗和构筑是否有趣；好玩了再用 Godot（方案 A）做正式版。不要一上来就纠结引擎和美术。

---

## 二、数据驱动设计（最关键的工程决策）

把「游戏内容」做成**数据**（JSON/资源文件），而不是写死在代码里。这是独立开发 + AI 辅助的核心技巧：

- 技能、遗物、敌人、事件全部用数据描述。
- 代码只写「如何执行这些数据」的引擎逻辑。
- 好处：加一个新技能 = 加一条数据，不用改代码；可以让 AI **批量生成大量内容数据**；调数值改 JSON 即可，迭代飞快。

### 技能数据示例（JSON）

```json
{
  "id": "skill_fireball",
  "name": "火球术",
  "ap_cost": 2,
  "target": "single_enemy",
  "effects": [
    { "type": "damage", "value": 8 },
    { "type": "apply_status", "status": "burn", "stacks": 3 }
  ],
  "tags": ["fire", "attack"]
}
```

### 遗物数据示例

```json
{
  "id": "relic_eternal_flame",
  "name": "不熄之焰",
  "description": "燃烧效果不再随回合衰减。",
  "triggers": [
    { "on": "turn_end", "modify": "burn_no_decay" }
  ]
}
```

### 敌人数据示例

```json
{
  "id": "enemy_shade",
  "name": "残影",
  "hp": 30,
  "intents": [
    { "weight": 60, "action": "attack", "value": 6 },
    { "weight": 40, "action": "defend", "value": 5 }
  ]
}
```

> 核心引擎要做的是：读取这些数据 → 在回合流程里执行 effects / triggers / intents。设计一套清晰的「效果类型枚举」（damage / heal / apply_status / draw / gain_ap...）是关键，效果种类先少而精，后续可扩展。

---

## 三、推荐的代码模块结构

不论用哪个技术栈，逻辑分层都建议这样（以便测试和 AI 分模块生成）：

```
/data            # 所有内容数据（JSON）：skills/ relics/ enemies/ events/
/core            # 纯逻辑，不依赖 UI/引擎（可独立单元测试）
  combat.ts        # 回合制战斗状态机
  effects.ts       # 效果系统（执行 damage/status/...）
  status.ts        # 状态效果（燃烧/中毒/护盾...）
  map.ts           # 节点图生成
  run.ts           # 单局流程（进度、货币、构筑）
  meta.ts          # 跨局养成存档（解锁、永久货币）
  rng.ts           # 种子化随机
/content         # 内容加载器（读 /data）
/ui              # 表现层（引擎/框架相关）
/save            # 存档读写
```

> **关键原则**：`/core` 必须是纯逻辑、可独立运行和单元测试的。这样你可以先不做任何 UI，直接写测试验证「战斗算得对不对、构筑组合是否生效」。这对 AI 辅助开发极其友好——逻辑正确性可以被自动验证。

---

## 四、MVP 拆解与开发顺序

按「先核心循环、后内容、再表现」的顺序，每一步都可独立验证。建议每完成一步就实际玩一玩。

### 阶段 0：纸面/文字验证（0.5-1 周）

- 用纸笔或 Excel 模拟一场战斗，确认数值框架不离谱。
- 可选：写个 Python 命令行版打一场，验证手感。

### 阶段 1：核心战斗（最重要）

- 实现回合制战斗状态机：玩家回合 / 敌人回合、AP、出招。
- 实现效果系统：伤害、护甲、3-5 种状态效果。
- 实现敌人意图系统。
- 用 3-4 个技能 + 2 种敌人打通一场战斗。
- **验收标准**：能完整打赢/打输一场战斗，数值正确。

### 阶段 2：单局循环

- 节点图生成（先做简单线性，再做分支）。
- 战斗奖励（三选一）、商店、营地、随机事件。
- 本局货币系统、构筑（技能/遗物的获取与生效）。
- **验收标准**：能从入夜走到 Boss，完整跑完一「夜」。

### 阶段 3：跨局养成与叙事壳

- 死亡/胜利结算 → 永久货币。
- 存档系统、解锁系统（新技能/遗物入池）。
- 接入最简叙事：入夜/死亡时的剧情文本、记忆碎片解锁。
- **验收标准**：死了能变强/解锁新内容，有「想再来一把」的感觉。

### 阶段 4：内容扩充与平衡

- 用数据驱动 + AI 批量扩充技能/遗物/敌人/事件到 §设计文档 §6.4 的 MVP 量。
- 调平衡：让至少 2-3 种构筑流派都可行。
- 加 NPC 羁绊系统、关键抉择。

### 阶段 5：表现与打磨

- 上美术（或先用免费素材/占位图）、音效、动画。
- UI/UX 打磨，新手引导。

> 阶段 1-3 是「这游戏好不好玩」的核心，全部不需要美术就能验证。**别在核心循环没验证之前投入美术**。

---

## 五、AI 辅助开发的实战建议

- **给 AI 喂上下文**：把《设计文档.md》和本文件作为上下文给 AI，它生成的代码会贴合你的设计。
- **分模块让 AI 写**：一次让 AI 实现一个 `/core` 模块（如 effects.ts），配上数据格式定义和测试用例，质量最高。
- **让 AI 批量生成内容数据**：定好 JSON schema 后，让 AI「按这个格式生成 10 个火系技能」，再人工筛选调整。
- **要求 AI 写单元测试**：核心逻辑让 AI 同时产出测试，保证战斗计算正确。
- **小步迭代**：每次只让 AI 改/加一小块，跑通验证再继续，避免一次性生成大量代码难以调试。

---

## 六、避坑提醒

- **范围蔓延是独立游戏头号杀手**：MVP 阶段坚决砍功能，先做出能玩的核心循环。
- **别过早优化美术**：占位图 + 好玩的核心 >> 精美画面 + 无聊玩法。
- **数值平衡是持续工作**：靠数据驱动让你能快速调，别指望一次调好。
- **存档兼容**：养成存档结构早点想清楚，后续加内容时尽量向后兼容（用版本号 + 迁移逻辑）。
- **种子化随机要趁早**：从一开始就用可控 RNG，否则后期调试随机性会很痛苦。
