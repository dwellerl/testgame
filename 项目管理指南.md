# 项目管理指南：如何用 AI 辅助开发而不让上下文爆炸

> 核心理念：**会话是临时的工作窗口，文件才是项目的永久记忆。** 不要试图在一个长会话里维持整个项目状态——那必然爆炸。正确做法是把记忆外置到文件，每个会话只做一件小事，做完写回文件，然后开新会话。

---

## 一、为什么会上下文爆炸，以及怎么根治

**爆炸的根源**：AI 会话的上下文窗口有限。聊得越久，早期的关键信息越容易被挤出、稀释、遗忘，AI 开始"失忆"、自相矛盾、重复犯错。

**根治思路（上下文工程）**：
- 会话 = 易失的、临时的、单任务的工作窗口
- 文件 = 持久的、权威的项目记忆
- 每个会话只做一个明确任务 → 做完把成果和决策写回文件 → **开新会话**做下一件事
- 新会话开始时，只读取它**这次需要**的那几个文件作为上下文

这样无论项目多大、做了多久，任何单个会话的上下文都很小，永远不会爆。

---

## 二、四个支柱

### 支柱 1：任务粒度要小（一个会话 = 一个小任务）

✅ 好的任务边界：
- "实现回合制战斗状态机（core/combat）"
- "实现效果系统的 damage / heal / apply_status 三种效果"
- "按 schema 生成 10 个火系技能数据并自检平衡"
- "修复燃烧状态叠加计算的 bug"

❌ 会爆炸的任务边界：
- "把战斗系统做出来"（太大）
- "继续做游戏"（无边界）
- 一个会话里又写战斗、又写地图、又调数值、又写剧情

**判断标准**：如果一个任务需要 AI 同时理解 3 个以上模块、或预计要来回几十轮对话，就拆小它。

### 支柱 2：用文件做项目记忆

本项目维护以下"记忆文件"（见本目录）：

| 文件 | 作用 | 谁更新 |
|---|---|---|
| `设计文档.md` | 玩法/系统设计（权威） | 设计变更时 |
| `技术实现与MVP路线.md` | 技术栈、架构、开发顺序 | 架构变更时 |
| `进度看板.md` | 现在在哪、下一步做什么、待办 | **每个会话结束时** |
| `决策日志.md` | 重要决策 + 为什么（避免反复纠结） | 做出关键决策时 |
| `AGENTS.md` | 给 AI 的项目说明书（规矩、约定） | 约定变更时 |

### 支柱 3：每个会话的固定仪式（开头注入、结尾沉淀）

**会话开头**（注入精准上下文）：
> "阅读 `AGENTS.md`、`进度看板.md`，以及【本次任务相关的文件，如 core/combat.ts 和 设计文档.md 的战斗章节】。本次任务：______。"

**会话结尾**（沉淀成果）：
> "更新 `进度看板.md`：标记完成项、记录下一步。如有关键决策，追加到 `决策日志.md`。"

不要省这两步。它们是防止失忆的关键。

### 支柱 4：让代码本身成为好上下文

- **数据驱动**：内容是 JSON 数据，AI 看 schema + 几个样例就能批量生成，不必读全部内容。
- **纯逻辑层 + 模块独立**：`/core` 每个文件职责单一，AI 读一个文件就懂一块功能。
- **单元测试**：测试既验证正确性，也是"这个模块该怎么用"的活文档。
- 这正是技术文档强调数据驱动 + 分层 + 测试的深层原因——**好的工程结构本身就在降低上下文成本**。

---

## 三、推荐的日常工作流

```
开新会话
  → 让 AI 读 AGENTS.md + 进度看板.md + 本次任务相关文件
  → 声明本次单一任务
  → AI 干活（小步迭代，跑测试验证）
  → 任务完成
  → 让 AI 更新 进度看板.md（+ 必要时 决策日志.md）
  → 结束会话
开新会话做下一件事……
```

**什么时候该开新会话？**
- 当前任务已完成 → 开新会话做下一个
- 感觉 AI 开始"失忆"/重复/答非所问 → 立刻沉淀进度，开新会话
- 切换到不相关的模块 → 开新会话（避免无关上下文堆积）

---

## 四、进阶技巧

- **用版本控制（Git）**：每完成一个任务就 commit，提交信息写清做了什么。Git 历史本身就是项目记忆，也方便回滚 AI 写错的代码。
- **里程碑式存档**：每完成一个 MVP 阶段，在 `进度看板.md` 归档该阶段总结，保持看板顶部只放"当前焦点"。
- **决策先记后做**：纠结的设计问题，先在 `决策日志.md` 写下选项和结论，避免下次会话又从头讨论一遍。
- **善用子任务并行**：互相独立的任务（如"生成技能数据"和"写地图生成逻辑"）可以在不同会话/不同时间分别做，互不污染上下文。
- **不要让 AI 一次读太多**：需要哪个文件读哪个，别为了"保险"把所有文件都塞进去——那等于人为制造上下文膨胀。
